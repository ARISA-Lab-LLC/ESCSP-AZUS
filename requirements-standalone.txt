# AZUS Standalone Upload Requirements
# No Prefect server or Prefect packages required.

# Core dependencies
pydantic>=2.0.0
requests>=2.28.0

# Optional: better HTML-to-Markdown conversion for README generation
# html2text>=2020.1.16
